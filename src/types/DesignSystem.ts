export type DesignSystem = {
  _id: string;
  name: string;
  isPublic: boolean;
  fontsId: string;
  paletteId: string;
  spacingsId: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
};